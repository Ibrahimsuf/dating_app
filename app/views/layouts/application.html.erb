<!DOCTYPE html>
<html class="h-full bg-white">
  <head>
    <title>DatingApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://demos.creative-tim.com/notus-js/assets/vendor/@fortawesome/fontawesome-free/css/all.min.css">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full">
    <main>
      <header class="bg-white">
        <nav class="mx-auto flex max-w-7xl items-center justify-between p-6 lg:px-8" aria-label="Global">
          <div class="flex lg:flex-1">
            <%= link_to(root_path, class: "flex items-center") do %>
              <%= image_tag("ace_of_spades.jpg", class: "h-10 w-auto") %>
              <h3 class="ml-2 text-2xl font-bold leading-9 tracking-tight text-gray-900">Ace Space</h3>
            <% end %>
          </div>
          <div>
          <% if user_signed_in? %>
            <%= button_to "logout", destroy_user_session_path, method: :delete , class: "bg-pink-500 active:bg-pink-600 uppercase text-white font-bold hover:shadow-md shadow text-xs px-4 py-2 rounded outline-none focus:outline-none sm:mr-2 mb-1 ease-linear transition-all duration-150" %>
          <% end %>
          </div>
        </div>
      </header>
      <%= yield %>
    </main>
  </body>
</html>
